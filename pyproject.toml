[project]
name = "echo-bot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "coverage>=7.6.12",
    "flask>=3.1.0",
    "flask-cors>=5.0.1",
]

[dependency-groups]
dev = [
    "coverage>=7.6.12",
]
